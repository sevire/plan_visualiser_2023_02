This folder was just created to have similar files to the other re-import files but with the activity_name columns as Task Name rather than Task which is what is expected for unit tests.

Should address this anomaly at some point!